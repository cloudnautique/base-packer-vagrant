# Kickstart file automatically generated by anaconda.

#version=DEVEL
install
cdrom
lang en_US.UTF-8
keyboard us
network --onboot yes --device eth0 --bootproto dhcp --noipv6
rootpw  --iscrypted $6$vwjiUkPZOliCM6WY$aPa0waT8cLU6lUG5UkxMRVmBv6xi.TtU9hQ/dz.Ijt8qTiM.TAtYNuSFv8LsgmVqt57EVDs5GFbIT8Tx8Zl4T0
firewall --service=ssh
authconfig --enableshadow --passalgo=sha512
selinux --disabled
timezone --utc America/Phoenix
bootloader --location=mbr --driveorder=sda --append="crashkernel=auto rhgb quiet"

text
skipx
firstboot --disabled

zerombr
clearpart --all --initlabel
autopart

reboot

%packages --nobase
@core

%post
## Setup sudo
cat > /etc/sudoers <<-"END_SUDOERS"
root    ALL =(ALL) ALL
%admin ALL=NOPASSWD: ALL
END_SUDOERS

chmod 440 /etc/sudoers
echo 'Defualts env_keep += "SSH_AUTH_SOCK"' > /etc/sudoers.d/auth_sock
chmod 440 /etc/sudoers.d/auth_sock

groupadd admin
useradd -m -U -G admin packer
echo "packer"|passwd --stdin packer

%end
